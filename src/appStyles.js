import { StyleSheet } from 'react-native'

export default StyleSheet.create({
  container: {
    flex: 1,
    justifyContent: 'center',
    alignItems: 'center'
  },
  timerContainer: {
    
  }
  buttonContainer: {
    borderColor: '#43a774',
    borderStyle: 'solid',
    borderWidth: 1,
    height: 40,
    width: 300,
  }
});
